upstream hello_world {
    server 127.0.0.1:3001;
}

server {
    listen 8001;
    server_name default_server;

    location / {
        uwsgi_pass  hello_world;
        include     /etc/nginx/uwsgi_params;
    }
}
